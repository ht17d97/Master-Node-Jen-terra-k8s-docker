apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "google-app.fullname" . }}-test-connection"
  labels:
    {{- include "google-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "google-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
