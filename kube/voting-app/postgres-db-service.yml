apiVersion: v1
kind: Service
metadata: 
  name: postgres-db-svc
  labels:
    name: postgres-db
    app: voting-application
spec: 
  ports: 
    - port: 5432
      targetPort: 5432
  selector: 
    name: postgres-db
    app: voting-application
    
