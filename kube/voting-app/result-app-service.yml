apiVersion: v1
kind: Service
metadata: 
  name: result-app-svc
  labels:
    name: result-app
    app: result-application
spec: 
  type: NodePort
  ports: 
    - port: 80
      targetPort: 80
      nodePort: 30005
  selector: 
    name: result-app
    app: result-application

        
